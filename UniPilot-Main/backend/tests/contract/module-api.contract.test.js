/**
 * Contract tests for module service APIs.
 * Minimal scaffold — add real contract checks as modules evolve.
 */

describe("Module API contracts", () => {
  test.todo(
    "CoreService returns profile fields when includeProfiles is set",
  );
  test.todo("AcademicService program/department lookup contract");
  test.todo("Fees service contract for fee status shape");
});
